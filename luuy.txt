1. khi clone về chạy nếu máy đang chạy wampserver thì phải tắt wampserver tránh đụng port không chạy được
2. làm bài nào phải sửa file connection lại cho phù hợp
3. lệnh chạy sau khi clone là docker compose up –build